import { LastnameSortPipe } from './lastname-sort.pipe';

describe('LastnameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
