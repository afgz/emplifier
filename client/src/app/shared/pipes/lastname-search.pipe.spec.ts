import { LastnameSearchPipe } from './lastname-search.pipe';

describe('LastnameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
